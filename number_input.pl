#!/usr/bin/perl
# use strict;
use warnings;
use utf8;
use warnings;
use diagnostics;

print "Enter the first number ";
$firstVal = <STDIN>;
print "Enter the second number ";
$secondVal = <STDIN>;

print "The product is " . $firstVal * $secondVal