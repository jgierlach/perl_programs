#!/usr/bin/perl
# use strict;
use warnings;
use utf8;
use warnings;
use diagnostics;

# chomp(@names = <STDIN>);
# @sortedNames = sort @names;
# foreach(@sortedNames) {
#   print $_ . ' ';    
# }

print sort <STDIN>;